<h1>Directory</h1>
<p>Name: {{ dirname }}</p>

<ul>
	{% for file in dirlisting %}
		{% if file.type == 'directory' %}
	<li><a href="{{ file.name }}/">{{ file.name|e }}</a></li>
		{% else %}
	<li>{{ file.name }}</li>
		{% endif %}
    {% endfor %}
</ul>
